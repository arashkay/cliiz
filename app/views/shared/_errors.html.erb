<% if obj.errors.any? %>
  <div class="failure">
    <h3><%= pluralize(obj.errors.count, "error") %></h3>
    <ul>
    <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
